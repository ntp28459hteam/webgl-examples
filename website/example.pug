
doctype html
html(lang = 'en')

  block vars

  include partials/head.pug

  body.bg-light
  
    include partials/navbar.pug

    section.container.mt-5
      block canvas
      p.float-left.text-left #{example.title} using #[a(href = 'https://github.com/cginternals/webgl-operate') webgl-operate].
      p.float-none.text-right by #{example.author}.


    section.container.mt-5
      div.row
        div.col
          h4 #{example.title} | Breadown

          block breakdown


    script(src = 'js/webgl-operate.js')
    script(src = example.target + '.js')
    include partials/foot.pug
